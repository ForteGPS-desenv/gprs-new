{% extends "layout.njk" %}

{% block content %}

<div class="card">
  <section 
        class="avatar" 
        style="background: url(../assets/{{member.name}}.jpg) no-repeat center center; background-size: cover;"
    ></section>
    <section class="details">
        <h3> Detalhes </h3>
        <div class="item">
            <div>Membro</div>
            <div>{{member.name}}</div>
        </div>
        <div class="item">
            <div>Tempo de ativação</div>
            <div>{{member.active}}</div>
        </div>
        <div class="item">
            <div>Responsavel</div>
            <div>
                {% for service in member.services %}
                    <span>{{service}}</span>
                {% endfor %}
                
            </div>
        </div>
        <div class="item">
            <div>Total de chip</div>
            <div>20</div>
            <div>Desde</div>
            <div>{{member.created_at}}</div>
        </div>
        <a href="#" class="button">Editar</a>
    </section>
</div>

{% endblock content%}